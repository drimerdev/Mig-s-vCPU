// Mig's vCPU
// version => 0.0.1 alpha #1
// env => nodejs
// script => javascript
// CPU(s) => null

var usr
var cpu
var emu
var initalize
var run
var num
var exec
var bin


// binary
function _bin() {
	if(hex = 0)
	if(hex = 1);
}

function _init() {
    // initial cpu set
if(hex = ('0')) {
        console.log('hex 0')
}

if(hex = ('1')) {
        console.log('hex 1')
}
};

function _conf() {
    // cpu main configuration
}

function _emu() {
    // cpu emulation manager
}
