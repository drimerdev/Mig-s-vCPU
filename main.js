// Mig's vCPU
// version => 0.0.1 alpha #1
// env => nodejs
// script => javascript
// CPU(s) => null

var usr
var clock
var cpu
var emu
var initalize
var hex
var run
var num
var exec
var bin

const clock = x
const hex = num
const { app } = require("./src/program.js");


// binary
function _bin() {
	if(hex = 0)
	if(hex = 1);
}

function _init() {
    // initial cpu set
};

function _conf() {
    // cpu main configuration
}

function _emu() {
    // cpu emulation manager
}
